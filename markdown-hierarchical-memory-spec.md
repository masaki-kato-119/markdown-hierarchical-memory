# Markdown階層記憶メモリ 仕様書（v1）

## 1. 基本思想

- 人間またはLLMがMarkdown形式で記憶を追加する。
- 記憶は単一ファイルではなく、関連性によって分割される。
- ファイル間リンク（Wikilink）によって知識ネットワークを形成する。
- 容量増大時は「要約して圧縮」または「詳細情報を別ファイルへ払い出す」のいずれかを選ぶ。
- 本設計は既存実装（hybrid-rag-memory MCP: チャンク＋メタデータ＋FAISS/BM25ハイブリッド検索）とは独立したトラックであり、統合は前提としない。

## 2. 記憶ファイル構造

```
memory/
 ├── index.md
 ├── projects/
 │    ├── hybrid_rag.md
 │    └── retrieval_strategy.md
 ├── concepts/
 │    ├── llm_memory.md
 │    └── rag.md
 └── archive/
      └── old_discussion.md
```

ディレクトリはあくまで補助であり、本質はMarkdown内部リンク。

```markdown
# HybridRAG

HybridRAGは検索精度向上のための構成。

詳細:
- [[retrieval_strategy]]
- [[graph_expansion]]

関連:
- [[LLM_Memory]]
```

## 3. Front Matter

```yaml
---
id: hybrid_rag
type: concept
importance: 0.8
pinned: false
created: 2026-08-04
updated: 2026-08-04
access_count: 52
last_access: 2026-08-04
parent:
  - llm_memory
tags:
  - rag
  - knowledge
---
```

## 4. 追加ルール

**Step1**: 現在のファイルへ追記可能か判断。条件＝同じテーマ／既存情報の補足／サイズ内。満たせば追記。

**Step2**: 関連性は高いが独立性がある場合、新規ファイルを作りリンクを張る（双方向リンクは§12参照）。

## 5. サイズ制御（目安）

| レベル | 行数 | 対応 |
|---|---|---|
| 1 | 100行以下 | そのまま |
| 2 | 100〜300行 | 章整理 |
| 3 | 300行以上 | §9の判断マトリクスに従い分割 or 要約 |

## 6〜8. （初期案の基本方針）

- 忘却は削除ではなくarchiveへの移動を基本とし、完全削除は禁止。
- 検索フローは「index.md → 関連Markdown探索 → リンク展開 → 必要部分取得 → 回答」。
- Vector検索 + MarkdownリンクGraph + メタ情報のハイブリッドとして機能する。

## 9. 判断軸の決定マトリクス（要約 vs 分割）

サイズ超過時、以下の組み合わせで判断材料を与える（§17によりLLMへのヒントであり厳密なif-elseではない）。

| サイズ | 意味的凝集度 | 参照頻度 | 重要度 | アクション |
|---|---|---|---|---|
| 超過 | 低（複数テーマ混在） | – | – | 分割 |
| 超過 | 高 | 高 | 高 | 章整理のみ（圧縮しない） |
| 超過 | 高 | 低 | 高 | 要約してarchiveへ |
| 超過 | 高 | 高 | 低 | 章整理のみ |
| 超過 | 高 | 低 | 低 | 要約して積極的にarchive |

分割は「意味的凝集度」主導、要約は「重要度×参照頻度」主導という役割分担が核。

## 10. Memory Manager Agent

```
トリガー: 追記操作の直後 or 定期バッチ
入力: 新規断片, 対象ファイル候補, 対象ファイルの現在のFront Matter
処理:
  A. 追記可否判定（既存テーマとの一致 + サイズ内か）
  B. 不可なら独立ファイル化 → 双方向リンクを同時作成（§12）
  C. 追記後サイズ閾値チェック → §9のマトリクスを判断材料として適用
  D. 実行（分割 / 要約 / 据え置き）
出力: 更新後Markdown群 + index.md更新 + 変更ログ
```

変更ログ（`archive/_manager_log.md`に追記形式）は判断根拠の事後検証・誤判定の手動差し戻しのために必須。

## 11. Front Matter更新の責任分担

- `access_count` / `last_access`：検索でファイルが実取得された時点で検索フロー側がインクリメント。
- `updated`：書き込み操作のたびに更新。
- `importance`：自動減衰させない。Manager Agentが下げる必要を感じてもarchive候補として提示するのみで、実行は明示確認を待つ。
- `parent` / `tags`：分割・リンク作成と同一トランザクションで更新。

## 12. リンクの双方向性

分割でA→Bが生まれる場合、以下2つの更新を不可分の1操作として扱う。

- A側：関連セクションに「詳細: [[B]]」を追加
- B側：Front Matterに`parent: [A]`を追加

Manager Agentの出力は「両方更新済みか」を自己チェックしてから確定する。片方のみの更新は孤立リンク・孤立ファイルを生むため許容しない。

## 13. 忘却モデル

```
archive条件: access_count < 3 AND last_access > 180日 AND pinned != true
```

`pinned`はデフォルトfalse、ユーザーが明示的に維持指定したファイルのみtrue（importanceとは独立軸）。archiveは物理的にディレクトリ移動するが、index.mdからのリンクは「(archived)」表示で残し、存在は可視化する。完全削除は禁止。

## 14. index.mdの役割

純粋なレジストリ（id・一行説明・tags）に限定し、本文を持たせない。index.md自体が肥大化した場合は「分類軸を見直せ」のサインとして扱い、`index/projects.md`のようにテーマ別へ分割する。

## 15. 検索時のリンク展開深さ

デフォルト1-hop（index→対象ファイル→直接リンク先の要約のみ）。それで不足と判断された場合のみ2-hop目へ。埋め込み類似度は候補発見の一次フィルタ、リンクグラフ距離は二次フィルタ。

## 16. 同時書き込み

フルロックは過剰。`updated`のタイムスタンプ（簡易バージョン値）を読み取り時に保持し、書き込み時に不一致なら再読込→再試行。

## 17. 判定のブレを許容する原則

- Manager Agentの「意味的凝集度」「重要度」判定は決定的アルゴリズムではなく都度のLLM推論であり、実行のたびに結果が変わりうることを前提とする。
- ブレへの対策は「判定の固定化」ではなく「後から修正可能にすること」に置く。§10の変更ログがその役割を担う。
- §9の決定マトリクスは厳密なif-elseではなく、LLMへの判断材料（ヒント）として与える。最終判断は毎回のプロンプト文脈でLLMが下す。
- この非決定性は人間の記憶が想起のたびに再構成される性質（reconstructive memory）に近く、「人間の記憶に近い」という当初の着想とも整合する。

## 未解決／今後の検討事項

- Manager Agentの実行タイミング（追記のたびに同期実行 vs 定期バッチ）の使い分け基準は未確定。
- 変更ログ自体が肥大化した場合の扱い（§9の適用対象に含めるか、専用ルールを作るか）は未検討。
